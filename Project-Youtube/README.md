# 📺 YouTube Clone Project

A responsive, high-fidelity clone of the YouTube homepage built using plain **HTML5** and **CSS3**. This project brings together all previous concepts into a real-world application.

## ✨ Features

- **Responsive Grid Layout**: Video thumbnails automatically adjust based on screen width using CSS Grid.
- **Flexbox Header & Navigation**: A sophisticated header with search, mic, and create icons, aligned using Flexbox.
- **Fixed Sidebar**: A navigation menu that stays fixed on the left side.
- **Hover Interactions**:
  - Detailed tooltips on hover.
  - Subtle background changes on buttons.
  - Video thumbnail scaling effects.
- **Semantic HTML**: Using proper tags (`<header>`, `<nav>`, `<main>`, `<section>`) for accessibility.

## 🛠️ Technologies Used

- **HTML5**: Structure and semantics.
- **CSS3**:
  - Flexbox (Header, Sidebar)
  - CSS Grid (Video Gallery)
  - Position: Absolute/Fixed (Tooltips, Overlays)
- **Google Fonts**: Roboto (Standard YouTube font).

## 🚀 How to Run

1. Clone the repository.
2. Navigate to `Project-Youtube/`.
3. Open `youtube.html` in your browser.

## 📸 Project Structure

```text
Project-Youtube/
├── icons/          # SVG icons for buttons and nav
├── images/         # Thumbnails and profile pictures
├── styles/         # Organized CSS files (header.css, general.css, video.css)
└── youtube.html    # Main entry point
```

---

_Based on the HTML & CSS Full Course by SuperSimpleDev._
