# 🔄 CSS Nested Flexbox

Flexbox becomes extremely powerful when you nest flex containers inside other flex containers. This allows intricate control over every part of the layout.

## 📝 Key Concepts Learned

### 1. **Layout Hierarchy**

- **Parent Flex**: Handles broad layout (e.g., header left vs. header right).
- **Child Flex**: Handles internal items (e.g., search icon + input).

### 2. **Space Distribution Strategy**

- Determining when to use `justify-content: space-between` vs. `margin-right: auto`.
- Shrinking and growing items (`flex-shrink`, `flex-grow`).

## 📂 Examples

- **YouTube Header**: Aligning the hamburger menu, logo, search bar, and notifications perfectly.
- **Twitter Post Actions**: Aligning reply, retweet, like, and share icons inside a tweet container.

---

_Part of the HTML & CSS Full Course Practice._
