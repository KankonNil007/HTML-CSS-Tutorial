# 📄 HTML Basics

Welcome to the **HTML Basics** module! This section focuses on the fundamental building blocks of the web—**HyperText Markup Language (HTML)**. Here, we explore how to structure content using tags and attributes.

## 📝 Key Concepts Learned

### 1. **HTML Structure**

- Understanding the syntax: `<tagname>Content</tagname>`.
- The difference between **opening tags** and **closing tags**.
- Using **attributes** to provide additional information about elements.

### 2. **Text Elements**

- **Headings**: Using `<h1>` through `<h6>` for titles and subtitles.
- **Paragraphs**: Structuring blocks of text with `<p>`.
- **Formatting**: Bold (`<strong>`), Italic (`<em>`).

### 3. **Interactive Elements**

- **Buttons**: Creating clickable buttons with `<button>`.
- **Links**: Navigating between pages using the anchor tag `<a>`.

### 4. **Attributes Deep Dive**

- `href`: Specifying the destination URL for links.
- `target`: Controlling where links open (e.g., `_blank` for a new tab).

## 📂 Featured Files

- `part-1.html`: Main lesson file covering buttons, links, and paragraphs.
- `1a.html` - `1g.html`: Exercise files practicing specific HTML concepts.

---

_Part of the HTML & CSS Full Course Practice._
